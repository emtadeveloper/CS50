/*
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
================================================================================================================================

install C to VSCode

1 - install  mingw  :::  sudo apt-get update   /   sudo apt-get -y install mingw-w64   /  sudo apt update       /    sudo apt -y install mingw-w64
                                 
2 - https://github.com/cs50/libcs50/archive/refs/tags/v10.1.0.tar.gz  دانلود 
    cd desktop/libcs50-10.1.0
    sudo make install
    #include <cs50.h>
    clang hello.c -o hello -lcs50

۳- make hello  /  clang hello.c -o hello -lcs50   /  ./hello 

================================================================================================================================
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
*/
